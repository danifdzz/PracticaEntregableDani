<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PracticaEntregable.MVVM.Views.AgregarTareaView"
             Title="Agregar Tarea">
    <StackLayout Padding="15">
        <Entry Text="{Binding Titulo}" Placeholder="Título" />
        <Entry Text="{Binding Importancia}" Placeholder="Importancia" />
        <DatePicker Date="{Binding FechaLimite}" />

        <Button Text="Guardar Tarea" Command="{Binding GuardarCommand}" />
    </StackLayout>
</ContentPage>
